// Generated by `wit-bindgen` 0.58.0. DO NOT EDIT!
///|
pub fn query(statement : String, params : Array[@types.ParameterValue]) -> Result[@types.RowSet, @types.Error_] {
      let cleanup_list : Array[Int] = []

      let ptr = mbt_ffi_str2ptr(statement)

      let address59 = mbt_ffi_malloc((params).length() * 56);
      for index60 = 0; index60 < (params).length(); index60 = index60 + 1 {
            let iter_elem : @types.ParameterValue = (params)[(index60)]
            let iter_base = address59 + (index60 * 56);

            match iter_elem {
                  Boolean(payload) => {
                        mbt_ffi_store8((iter_base) + 0, (0))
                        mbt_ffi_store8((iter_base) + 8, (if payload { 1 } else { 0 }))

                        ()
                  }
                  Int8(payload0) => {
                        mbt_ffi_store8((iter_base) + 0, (1))
                        mbt_ffi_store8((iter_base) + 8, mbt_ffi_extend8(payload0))

                        ()
                  }
                  Int16(payload1) => {
                        mbt_ffi_store8((iter_base) + 0, (2))
                        mbt_ffi_store16((iter_base) + 8, mbt_ffi_extend16(payload1))

                        ()
                  }
                  Int32(payload2) => {
                        mbt_ffi_store8((iter_base) + 0, (3))
                        mbt_ffi_store32((iter_base) + 8, payload2)

                        ()
                  }
                  Int64(payload3) => {
                        mbt_ffi_store8((iter_base) + 0, (4))
                        mbt_ffi_store64((iter_base) + 8, payload3)

                        ()
                  }
                  Floating32(payload4) => {
                        mbt_ffi_store8((iter_base) + 0, (5))
                        mbt_ffi_storef32((iter_base) + 8, payload4)

                        ()
                  }
                  Floating64(payload5) => {
                        mbt_ffi_store8((iter_base) + 0, (6))
                        mbt_ffi_storef64((iter_base) + 8, payload5)

                        ()
                  }
                  Str(payload6) => {
                        mbt_ffi_store8((iter_base) + 0, (7))

                        let ptr7 = mbt_ffi_str2ptr(payload6)
                        mbt_ffi_store32((iter_base) + 12, payload6.length())
                        mbt_ffi_store32((iter_base) + 8, ptr7)
                        cleanup_list.push(ptr7)

                        ()
                  }
                  Binary(payload8) => {
                        mbt_ffi_store8((iter_base) + 0, (8))

                        let ptr9 = mbt_ffi_bytes2ptr(payload8)

                        mbt_ffi_store32((iter_base) + 12, payload8.length())
                        mbt_ffi_store32((iter_base) + 8, ptr9)
                        cleanup_list.push(ptr9)

                        ()
                  }
                  Date(payload10) => {
                        mbt_ffi_store8((iter_base) + 0, (9))
                        mbt_ffi_store32((iter_base) + 8, (payload10).0)
                        mbt_ffi_store8((iter_base) + 12, ((payload10).1).to_int())
                        mbt_ffi_store8((iter_base) + 13, ((payload10).2).to_int())

                        ()
                  }
                  Time(payload11) => {
                        mbt_ffi_store8((iter_base) + 0, (10))
                        mbt_ffi_store8((iter_base) + 8, ((payload11).0).to_int())
                        mbt_ffi_store8((iter_base) + 9, ((payload11).1).to_int())
                        mbt_ffi_store8((iter_base) + 10, ((payload11).2).to_int())
                        mbt_ffi_store32((iter_base) + 12, ((payload11).3).reinterpret_as_int())

                        ()
                  }
                  Datetime(payload12) => {
                        mbt_ffi_store8((iter_base) + 0, (11))
                        mbt_ffi_store32((iter_base) + 8, (payload12).0)
                        mbt_ffi_store8((iter_base) + 12, ((payload12).1).to_int())
                        mbt_ffi_store8((iter_base) + 13, ((payload12).2).to_int())
                        mbt_ffi_store8((iter_base) + 14, ((payload12).3).to_int())
                        mbt_ffi_store8((iter_base) + 15, ((payload12).4).to_int())
                        mbt_ffi_store8((iter_base) + 16, ((payload12).5).to_int())
                        mbt_ffi_store32((iter_base) + 20, ((payload12).6).reinterpret_as_int())

                        ()
                  }
                  Timestamp(payload13) => {
                        mbt_ffi_store8((iter_base) + 0, (12))
                        mbt_ffi_store64((iter_base) + 8, payload13)

                        ()
                  }
                  Uuid(payload14) => {
                        mbt_ffi_store8((iter_base) + 0, (13))

                        let ptr15 = mbt_ffi_str2ptr(payload14)
                        mbt_ffi_store32((iter_base) + 12, payload14.length())
                        mbt_ffi_store32((iter_base) + 8, ptr15)
                        cleanup_list.push(ptr15)

                        ()
                  }
                  Jsonb(payload16) => {
                        mbt_ffi_store8((iter_base) + 0, (14))

                        let ptr17 = mbt_ffi_bytes2ptr(payload16)

                        mbt_ffi_store32((iter_base) + 12, payload16.length())
                        mbt_ffi_store32((iter_base) + 8, ptr17)
                        cleanup_list.push(ptr17)

                        ()
                  }
                  Decimal(payload18) => {
                        mbt_ffi_store8((iter_base) + 0, (15))

                        let ptr19 = mbt_ffi_str2ptr(payload18)
                        mbt_ffi_store32((iter_base) + 12, payload18.length())
                        mbt_ffi_store32((iter_base) + 8, ptr19)
                        cleanup_list.push(ptr19)

                        ()
                  }
                  RangeInt32(payload20) => {
                        mbt_ffi_store8((iter_base) + 0, (16))

                        match ((payload20).0) {
                              None => {
                                    mbt_ffi_store8((iter_base) + 8, (0))

                                    ()
                              }
                              Some(payload22) => {
                                    mbt_ffi_store8((iter_base) + 8, (1))
                                    mbt_ffi_store32((iter_base) + 12, (payload22).0)
                                    mbt_ffi_store8((iter_base) + 16, (payload22).1.ordinal())

                                    ()
                              }
                        }

                        match ((payload20).1) {
                              None => {
                                    mbt_ffi_store8((iter_base) + 20, (0))

                                    ()
                              }
                              Some(payload24) => {
                                    mbt_ffi_store8((iter_base) + 20, (1))
                                    mbt_ffi_store32((iter_base) + 24, (payload24).0)
                                    mbt_ffi_store8((iter_base) + 28, (payload24).1.ordinal())

                                    ()
                              }
                        }

                        ()
                  }
                  RangeInt64(payload25) => {
                        mbt_ffi_store8((iter_base) + 0, (17))

                        match ((payload25).0) {
                              None => {
                                    mbt_ffi_store8((iter_base) + 8, (0))

                                    ()
                              }
                              Some(payload27) => {
                                    mbt_ffi_store8((iter_base) + 8, (1))
                                    mbt_ffi_store64((iter_base) + 16, (payload27).0)
                                    mbt_ffi_store8((iter_base) + 24, (payload27).1.ordinal())

                                    ()
                              }
                        }

                        match ((payload25).1) {
                              None => {
                                    mbt_ffi_store8((iter_base) + 32, (0))

                                    ()
                              }
                              Some(payload29) => {
                                    mbt_ffi_store8((iter_base) + 32, (1))
                                    mbt_ffi_store64((iter_base) + 40, (payload29).0)
                                    mbt_ffi_store8((iter_base) + 48, (payload29).1.ordinal())

                                    ()
                              }
                        }

                        ()
                  }
                  RangeDecimal(payload30) => {
                        mbt_ffi_store8((iter_base) + 0, (18))

                        match ((payload30).0) {
                              None => {
                                    mbt_ffi_store8((iter_base) + 8, (0))

                                    ()
                              }
                              Some(payload32) => {
                                    mbt_ffi_store8((iter_base) + 8, (1))

                                    let ptr33 = mbt_ffi_str2ptr((payload32).0)
                                    mbt_ffi_store32((iter_base) + 16, (payload32).0.length())
                                    mbt_ffi_store32((iter_base) + 12, ptr33)
                                    mbt_ffi_store8((iter_base) + 20, (payload32).1.ordinal())
                                    cleanup_list.push(ptr33)

                                    ()
                              }
                        }

                        match ((payload30).1) {
                              None => {
                                    mbt_ffi_store8((iter_base) + 24, (0))

                                    ()
                              }
                              Some(payload35) => {
                                    mbt_ffi_store8((iter_base) + 24, (1))

                                    let ptr36 = mbt_ffi_str2ptr((payload35).0)
                                    mbt_ffi_store32((iter_base) + 32, (payload35).0.length())
                                    mbt_ffi_store32((iter_base) + 28, ptr36)
                                    mbt_ffi_store8((iter_base) + 36, (payload35).1.ordinal())
                                    cleanup_list.push(ptr36)

                                    ()
                              }
                        }

                        ()
                  }
                  ArrayInt32(payload37) => {
                        mbt_ffi_store8((iter_base) + 0, (19))

                        let address = mbt_ffi_malloc((payload37).length() * 8);
                        for index = 0; index < (payload37).length(); index = index + 1 {
                              let iter_elem : Int? = (payload37)[(index)]
                              let iter_base = address + (index * 8);

                              match (iter_elem) {
                                    None => {
                                          mbt_ffi_store8((iter_base) + 0, (0))

                                          ()
                                    }
                                    Some(payload39) => {
                                          mbt_ffi_store8((iter_base) + 0, (1))
                                          mbt_ffi_store32((iter_base) + 4, payload39)

                                          ()
                                    }
                              }

                        }
                        mbt_ffi_store32((iter_base) + 12, (payload37).length())
                        mbt_ffi_store32((iter_base) + 8, address)
                        cleanup_list.push(address)

                        ()
                  }
                  ArrayInt64(payload40) => {
                        mbt_ffi_store8((iter_base) + 0, (20))

                        let address43 = mbt_ffi_malloc((payload40).length() * 16);
                        for index44 = 0; index44 < (payload40).length(); index44 = index44 + 1 {
                              let iter_elem : Int64? = (payload40)[(index44)]
                              let iter_base = address43 + (index44 * 16);

                              match (iter_elem) {
                                    None => {
                                          mbt_ffi_store8((iter_base) + 0, (0))

                                          ()
                                    }
                                    Some(payload42) => {
                                          mbt_ffi_store8((iter_base) + 0, (1))
                                          mbt_ffi_store64((iter_base) + 8, payload42)

                                          ()
                                    }
                              }

                        }
                        mbt_ffi_store32((iter_base) + 12, (payload40).length())
                        mbt_ffi_store32((iter_base) + 8, address43)
                        cleanup_list.push(address43)

                        ()
                  }
                  ArrayDecimal(payload45) => {
                        mbt_ffi_store8((iter_base) + 0, (21))

                        let address49 = mbt_ffi_malloc((payload45).length() * 12);
                        for index50 = 0; index50 < (payload45).length(); index50 = index50 + 1 {
                              let iter_elem : String? = (payload45)[(index50)]
                              let iter_base = address49 + (index50 * 12);

                              match (iter_elem) {
                                    None => {
                                          mbt_ffi_store8((iter_base) + 0, (0))

                                          ()
                                    }
                                    Some(payload47) => {
                                          mbt_ffi_store8((iter_base) + 0, (1))

                                          let ptr48 = mbt_ffi_str2ptr(payload47)
                                          mbt_ffi_store32((iter_base) + 8, payload47.length())
                                          mbt_ffi_store32((iter_base) + 4, ptr48)
                                          cleanup_list.push(ptr48)

                                          ()
                                    }
                              }

                        }
                        mbt_ffi_store32((iter_base) + 12, (payload45).length())
                        mbt_ffi_store32((iter_base) + 8, address49)
                        cleanup_list.push(address49)

                        ()
                  }
                  ArrayStr(payload51) => {
                        mbt_ffi_store8((iter_base) + 0, (22))

                        let address55 = mbt_ffi_malloc((payload51).length() * 12);
                        for index56 = 0; index56 < (payload51).length(); index56 = index56 + 1 {
                              let iter_elem : String? = (payload51)[(index56)]
                              let iter_base = address55 + (index56 * 12);

                              match (iter_elem) {
                                    None => {
                                          mbt_ffi_store8((iter_base) + 0, (0))

                                          ()
                                    }
                                    Some(payload53) => {
                                          mbt_ffi_store8((iter_base) + 0, (1))

                                          let ptr54 = mbt_ffi_str2ptr(payload53)
                                          mbt_ffi_store32((iter_base) + 8, payload53.length())
                                          mbt_ffi_store32((iter_base) + 4, ptr54)
                                          cleanup_list.push(ptr54)

                                          ()
                                    }
                              }

                        }
                        mbt_ffi_store32((iter_base) + 12, (payload51).length())
                        mbt_ffi_store32((iter_base) + 8, address55)
                        cleanup_list.push(address55)

                        ()
                  }
                  Interval(payload57) => {
                        mbt_ffi_store8((iter_base) + 0, (23))
                        mbt_ffi_store64((iter_base) + 8, (payload57).micros)
                        mbt_ffi_store32((iter_base) + 16, (payload57).days)
                        mbt_ffi_store32((iter_base) + 20, (payload57).months)

                        ()
                  }
                  DbNull => {
                        mbt_ffi_store8((iter_base) + 0, (24))

                        ()
                  }
            }

      }
      let return_area = mbt_ffi_malloc(64)
      wasmImportQuery(ptr, statement.length(), address59, (params).length(), return_area);

      let lifted113 = match (mbt_ffi_load8_u((return_area) + 0)) {
            0 => {

                  let array : Array[@types.Column] = [];
                  for index62 = 0; index62 < (mbt_ffi_load32((return_area) + 8)); index62 = index62 + 1 {
                        let iter_base = (mbt_ffi_load32((return_area) + 4)) + (index62 * 20)

                        let result = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 0), mbt_ffi_load32((iter_base) + 4))

                        let lifted = match (mbt_ffi_load8_u((iter_base) + 8)) {
                              0 => {

                                    @types.DbDataType::Boolean
                              }
                              1 => {

                                    @types.DbDataType::Int8
                              }
                              2 => {

                                    @types.DbDataType::Int16
                              }
                              3 => {

                                    @types.DbDataType::Int32
                              }
                              4 => {

                                    @types.DbDataType::Int64
                              }
                              5 => {

                                    @types.DbDataType::Floating32
                              }
                              6 => {

                                    @types.DbDataType::Floating64
                              }
                              7 => {

                                    @types.DbDataType::Str
                              }
                              8 => {

                                    @types.DbDataType::Binary
                              }
                              9 => {

                                    @types.DbDataType::Date
                              }
                              10 => {

                                    @types.DbDataType::Time
                              }
                              11 => {

                                    @types.DbDataType::Datetime
                              }
                              12 => {

                                    @types.DbDataType::Timestamp
                              }
                              13 => {

                                    @types.DbDataType::Uuid
                              }
                              14 => {

                                    @types.DbDataType::Jsonb
                              }
                              15 => {

                                    @types.DbDataType::Decimal
                              }
                              16 => {

                                    @types.DbDataType::RangeInt32
                              }
                              17 => {

                                    @types.DbDataType::RangeInt64
                              }
                              18 => {

                                    @types.DbDataType::RangeDecimal
                              }
                              19 => {

                                    @types.DbDataType::ArrayInt32
                              }
                              20 => {

                                    @types.DbDataType::ArrayInt64
                              }
                              21 => {

                                    @types.DbDataType::ArrayDecimal
                              }
                              22 => {

                                    @types.DbDataType::ArrayStr
                              }
                              23 => {

                                    @types.DbDataType::Interval
                              }
                              24 => {

                                    let result61 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 12), mbt_ffi_load32((iter_base) + 16))

                                    @types.DbDataType::Other(result61)
                              }
                              _ => panic()
                        }

                        array.push(@types.Column::{name : result, data_type : lifted})
                  }
                  mbt_ffi_free(mbt_ffi_load32((return_area) + 4))

                  let array94 : Array[Array[@types.DbValue]] = [];
                  for index95 = 0; index95 < (mbt_ffi_load32((return_area) + 16)); index95 = index95 + 1 {
                        let iter_base = (mbt_ffi_load32((return_area) + 12)) + (index95 * 8)

                        let array92 : Array[@types.DbValue] = [];
                        for index93 = 0; index93 < (mbt_ffi_load32((iter_base) + 4)); index93 = index93 + 1 {
                              let iter_base = (mbt_ffi_load32((iter_base) + 0)) + (index93 * 56)

                              let lifted91 = match (mbt_ffi_load8_u((iter_base) + 0)) {
                                    0 => {

                                          @types.DbValue::Boolean((mbt_ffi_load8_u((iter_base) + 8) != 0))
                                    }
                                    1 => {

                                          @types.DbValue::Int8((mbt_ffi_load8((iter_base) + 8) - 0x100))
                                    }
                                    2 => {

                                          @types.DbValue::Int16((mbt_ffi_load16((iter_base) + 8) - 0x10000))
                                    }
                                    3 => {

                                          @types.DbValue::Int32(mbt_ffi_load32((iter_base) + 8))
                                    }
                                    4 => {

                                          @types.DbValue::Int64(mbt_ffi_load64((iter_base) + 8))
                                    }
                                    5 => {

                                          @types.DbValue::Floating32(mbt_ffi_loadf32((iter_base) + 8))
                                    }
                                    6 => {

                                          @types.DbValue::Floating64(mbt_ffi_loadf64((iter_base) + 8))
                                    }
                                    7 => {

                                          let result63 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                                          @types.DbValue::Str(result63)
                                    }
                                    8 => {

                                          let result64 = mbt_ffi_ptr2bytes(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                                          @types.DbValue::Binary(result64)
                                    }
                                    9 => {

                                          @types.DbValue::Date((mbt_ffi_load32((iter_base) + 8), (mbt_ffi_load8_u((iter_base) + 12)).to_byte(), (mbt_ffi_load8_u((iter_base) + 13)).to_byte()))
                                    }
                                    10 => {

                                          @types.DbValue::Time(((mbt_ffi_load8_u((iter_base) + 8)).to_byte(), (mbt_ffi_load8_u((iter_base) + 9)).to_byte(), (mbt_ffi_load8_u((iter_base) + 10)).to_byte(), (mbt_ffi_load32((iter_base) + 12)).reinterpret_as_uint()))
                                    }
                                    11 => {

                                          @types.DbValue::Datetime((mbt_ffi_load32((iter_base) + 8), (mbt_ffi_load8_u((iter_base) + 12)).to_byte(), (mbt_ffi_load8_u((iter_base) + 13)).to_byte(), (mbt_ffi_load8_u((iter_base) + 14)).to_byte(), (mbt_ffi_load8_u((iter_base) + 15)).to_byte(), (mbt_ffi_load8_u((iter_base) + 16)).to_byte(), (mbt_ffi_load32((iter_base) + 20)).reinterpret_as_uint()))
                                    }
                                    12 => {

                                          @types.DbValue::Timestamp(mbt_ffi_load64((iter_base) + 8))
                                    }
                                    13 => {

                                          let result65 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                                          @types.DbValue::Uuid(result65)
                                    }
                                    14 => {

                                          let result66 = mbt_ffi_ptr2bytes(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                                          @types.DbValue::Jsonb(result66)
                                    }
                                    15 => {

                                          let result67 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                                          @types.DbValue::Decimal(result67)
                                    }
                                    16 => {

                                          let lifted68 : (Int, @types.RangeBoundKind)? = match mbt_ffi_load8_u((iter_base) + 8) {
                                                0 => Option::None
                                                1 => {

                                                      Option::Some((mbt_ffi_load32((iter_base) + 12), @types.RangeBoundKind::from(mbt_ffi_load8_u((iter_base) + 16))))
                                                }
                                                _ => panic()
                                          }

                                          let lifted69 : (Int, @types.RangeBoundKind)? = match mbt_ffi_load8_u((iter_base) + 20) {
                                                0 => Option::None
                                                1 => {

                                                      Option::Some((mbt_ffi_load32((iter_base) + 24), @types.RangeBoundKind::from(mbt_ffi_load8_u((iter_base) + 28))))
                                                }
                                                _ => panic()
                                          }

                                          @types.DbValue::RangeInt32((lifted68, lifted69))
                                    }
                                    17 => {

                                          let lifted70 : (Int64, @types.RangeBoundKind)? = match mbt_ffi_load8_u((iter_base) + 8) {
                                                0 => Option::None
                                                1 => {

                                                      Option::Some((mbt_ffi_load64((iter_base) + 16), @types.RangeBoundKind::from(mbt_ffi_load8_u((iter_base) + 24))))
                                                }
                                                _ => panic()
                                          }

                                          let lifted71 : (Int64, @types.RangeBoundKind)? = match mbt_ffi_load8_u((iter_base) + 32) {
                                                0 => Option::None
                                                1 => {

                                                      Option::Some((mbt_ffi_load64((iter_base) + 40), @types.RangeBoundKind::from(mbt_ffi_load8_u((iter_base) + 48))))
                                                }
                                                _ => panic()
                                          }

                                          @types.DbValue::RangeInt64((lifted70, lifted71))
                                    }
                                    18 => {

                                          let lifted73 : (String, @types.RangeBoundKind)? = match mbt_ffi_load8_u((iter_base) + 8) {
                                                0 => Option::None
                                                1 => {

                                                      let result72 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 12), mbt_ffi_load32((iter_base) + 16))

                                                      Option::Some((result72, @types.RangeBoundKind::from(mbt_ffi_load8_u((iter_base) + 20))))
                                                }
                                                _ => panic()
                                          }

                                          let lifted75 : (String, @types.RangeBoundKind)? = match mbt_ffi_load8_u((iter_base) + 24) {
                                                0 => Option::None
                                                1 => {

                                                      let result74 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 28), mbt_ffi_load32((iter_base) + 32))

                                                      Option::Some((result74, @types.RangeBoundKind::from(mbt_ffi_load8_u((iter_base) + 36))))
                                                }
                                                _ => panic()
                                          }

                                          @types.DbValue::RangeDecimal((lifted73, lifted75))
                                    }
                                    19 => {

                                          let array77 : Array[Int?] = [];
                                          for index78 = 0; index78 < (mbt_ffi_load32((iter_base) + 12)); index78 = index78 + 1 {
                                                let iter_base = (mbt_ffi_load32((iter_base) + 8)) + (index78 * 8)

                                                let lifted76 : Int? = match mbt_ffi_load8_u((iter_base) + 0) {
                                                      0 => Option::None
                                                      1 => {

                                                            Option::Some(mbt_ffi_load32((iter_base) + 4))
                                                      }
                                                      _ => panic()
                                                }

                                                array77.push(lifted76)
                                          }
                                          mbt_ffi_free(mbt_ffi_load32((iter_base) + 8))

                                          @types.DbValue::ArrayInt32(array77)
                                    }
                                    20 => {

                                          let array80 : Array[Int64?] = [];
                                          for index81 = 0; index81 < (mbt_ffi_load32((iter_base) + 12)); index81 = index81 + 1 {
                                                let iter_base = (mbt_ffi_load32((iter_base) + 8)) + (index81 * 16)

                                                let lifted79 : Int64? = match mbt_ffi_load8_u((iter_base) + 0) {
                                                      0 => Option::None
                                                      1 => {

                                                            Option::Some(mbt_ffi_load64((iter_base) + 8))
                                                      }
                                                      _ => panic()
                                                }

                                                array80.push(lifted79)
                                          }
                                          mbt_ffi_free(mbt_ffi_load32((iter_base) + 8))

                                          @types.DbValue::ArrayInt64(array80)
                                    }
                                    21 => {

                                          let array84 : Array[String?] = [];
                                          for index85 = 0; index85 < (mbt_ffi_load32((iter_base) + 12)); index85 = index85 + 1 {
                                                let iter_base = (mbt_ffi_load32((iter_base) + 8)) + (index85 * 12)

                                                let lifted83 : String? = match mbt_ffi_load8_u((iter_base) + 0) {
                                                      0 => Option::None
                                                      1 => {

                                                            let result82 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 4), mbt_ffi_load32((iter_base) + 8))

                                                            Option::Some(result82)
                                                      }
                                                      _ => panic()
                                                }

                                                array84.push(lifted83)
                                          }
                                          mbt_ffi_free(mbt_ffi_load32((iter_base) + 8))

                                          @types.DbValue::ArrayDecimal(array84)
                                    }
                                    22 => {

                                          let array88 : Array[String?] = [];
                                          for index89 = 0; index89 < (mbt_ffi_load32((iter_base) + 12)); index89 = index89 + 1 {
                                                let iter_base = (mbt_ffi_load32((iter_base) + 8)) + (index89 * 12)

                                                let lifted87 : String? = match mbt_ffi_load8_u((iter_base) + 0) {
                                                      0 => Option::None
                                                      1 => {

                                                            let result86 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 4), mbt_ffi_load32((iter_base) + 8))

                                                            Option::Some(result86)
                                                      }
                                                      _ => panic()
                                                }

                                                array88.push(lifted87)
                                          }
                                          mbt_ffi_free(mbt_ffi_load32((iter_base) + 8))

                                          @types.DbValue::ArrayStr(array88)
                                    }
                                    23 => {

                                          @types.DbValue::Interval(@types.Interval::{micros : mbt_ffi_load64((iter_base) + 8), days : mbt_ffi_load32((iter_base) + 16), months : mbt_ffi_load32((iter_base) + 20)})
                                    }
                                    24 => {

                                          @types.DbValue::DbNull
                                    }
                                    25 => {

                                          let result90 = mbt_ffi_ptr2bytes(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                                          @types.DbValue::Unsupported(result90)
                                    }
                                    _ => panic()
                              }

                              array92.push(lifted91)
                        }
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 0))

                        array94.push(array92)
                  }
                  mbt_ffi_free(mbt_ffi_load32((return_area) + 12))

                  Result::Ok(@types.RowSet::{columns : array, rows : array94})
            }
            1 => {

                  let lifted112 = match (mbt_ffi_load8_u((return_area) + 4)) {
                        0 => {

                              let result96 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 8), mbt_ffi_load32((return_area) + 12))

                              @types.Error_::ConnectionFailed(result96)
                        }
                        1 => {

                              let result97 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 8), mbt_ffi_load32((return_area) + 12))

                              @types.Error_::BadParameter(result97)
                        }
                        2 => {

                              let lifted109 = match (mbt_ffi_load8_u((return_area) + 8)) {
                                    0 => {

                                          let result98 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 12), mbt_ffi_load32((return_area) + 16))

                                          @types.QueryError::Text(result98)
                                    }
                                    1 => {

                                          let result99 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 12), mbt_ffi_load32((return_area) + 16))

                                          let result100 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 20), mbt_ffi_load32((return_area) + 24))

                                          let result101 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 28), mbt_ffi_load32((return_area) + 32))

                                          let result102 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 36), mbt_ffi_load32((return_area) + 40))

                                          let lifted104 : String? = match mbt_ffi_load8_u((return_area) + 44) {
                                                0 => Option::None
                                                1 => {

                                                      let result103 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 48), mbt_ffi_load32((return_area) + 52))

                                                      Option::Some(result103)
                                                }
                                                _ => panic()
                                          }

                                          let array107 : Array[(String, String)] = [];
                                          for index108 = 0; index108 < (mbt_ffi_load32((return_area) + 60)); index108 = index108 + 1 {
                                                let iter_base = (mbt_ffi_load32((return_area) + 56)) + (index108 * 16)

                                                let result105 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 0), mbt_ffi_load32((iter_base) + 4))

                                                let result106 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

                                                array107.push((result105, result106))
                                          }
                                          mbt_ffi_free(mbt_ffi_load32((return_area) + 56))

                                          @types.QueryError::DbError(@types.DbError::{as_text : result99, severity : result100, code : result101, message : result102, detail : lifted104, extras : array107})
                                    }
                                    _ => panic()
                              }

                              @types.Error_::QueryFailed(lifted109)
                        }
                        3 => {

                              let result110 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 8), mbt_ffi_load32((return_area) + 12))

                              @types.Error_::ValueConversionFailed(result110)
                        }
                        4 => {

                              let result111 = mbt_ffi_ptr2str(mbt_ffi_load32((return_area) + 8), mbt_ffi_load32((return_area) + 12))

                              @types.Error_::Other(result111)
                        }
                        _ => panic()
                  }

                  Result::Err(lifted112)
            }
            _ => panic()
      }
      let ret = lifted113
      mbt_ffi_free(ptr)
      mbt_ffi_free(address59)
      mbt_ffi_free(return_area)

      cleanup_list.each(mbt_ffi_free)
      return ret

}

