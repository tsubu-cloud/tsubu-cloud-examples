// Generated by `wit-bindgen` 0.58.0. DO NOT EDIT!
///|
pub(all) struct DbError {
      as_text : String; severity : String; code : String; message : String; detail : String?; extras : Array[(String, String)]
} derive()
///|
pub(all) enum QueryError {
      Text(String)
      DbError(DbError)
} derive()
///|
/// Errors related to interacting with a database.
pub(all) enum Error_ {
      ConnectionFailed(String)
      BadParameter(String)
      QueryFailed(QueryError)
      ValueConversionFailed(String)
      Other(String)
} derive()
///|
/// Data types for a database column
pub(all) enum DbDataType {
      Boolean
      Int8
      Int16
      Int32
      Int64
      Floating32
      Floating64
      Str
      Binary
      Date
      Time
      Datetime
      Timestamp
      Uuid
      Jsonb
      Decimal
      RangeInt32
      RangeInt64
      RangeDecimal
      ArrayInt32
      ArrayInt64
      ArrayDecimal
      ArrayStr
      Interval
      Other(String)
} derive()
///|
pub(all) struct Interval {
      micros : Int64; days : Int; months : Int
} derive()
///|
/// A database column
pub(all) struct Column {
      name : String; data_type : DbDataType
} derive()
///|
/// For range types, indicates if each bound is inclusive or exclusive
pub(all) enum RangeBoundKind {
      INCLUSIVE; EXCLUSIVE
} derive()

pub fn RangeBoundKind::ordinal(self : RangeBoundKind) -> Int {
      match self {
            INCLUSIVE => 0
            EXCLUSIVE => 1
      }
}

pub fn RangeBoundKind::from(self : Int) -> RangeBoundKind {
      match self {
            0 => INCLUSIVE
            1 => EXCLUSIVE
            _ => panic()
      }
}
///|
/// Database values
pub(all) enum DbValue {
      Boolean(Bool)
      Int8(Int)
      Int16(Int)
      Int32(Int)
      Int64(Int64)
      Floating32(Float)
      Floating64(Double)
      Str(String)
      Binary(FixedArray[Byte])
      Date((Int, Byte, Byte))
      Time((Byte, Byte, Byte, UInt))
      Datetime((Int, Byte, Byte, Byte, Byte, Byte, UInt))
      Timestamp(Int64)
      Uuid(String)
      Jsonb(FixedArray[Byte])
      Decimal(String)
      RangeInt32(((Int, RangeBoundKind)?, (Int, RangeBoundKind)?))
      RangeInt64(((Int64, RangeBoundKind)?, (Int64, RangeBoundKind)?))
      RangeDecimal(((String, RangeBoundKind)?, (String, RangeBoundKind)?))
      ArrayInt32(Array[Int?])
      ArrayInt64(Array[Int64?])
      ArrayDecimal(Array[String?])
      ArrayStr(Array[String?])
      Interval(Interval)
      DbNull
      Unsupported(FixedArray[Byte])
} derive()
///|
/// Values used in parameterized queries
pub(all) enum ParameterValue {
      Boolean(Bool)
      Int8(Int)
      Int16(Int)
      Int32(Int)
      Int64(Int64)
      Floating32(Float)
      Floating64(Double)
      Str(String)
      Binary(FixedArray[Byte])
      Date((Int, Byte, Byte))
      Time((Byte, Byte, Byte, UInt))
      Datetime((Int, Byte, Byte, Byte, Byte, Byte, UInt))
      Timestamp(Int64)
      Uuid(String)
      Jsonb(FixedArray[Byte])
      Decimal(String)
      RangeInt32(((Int, RangeBoundKind)?, (Int, RangeBoundKind)?))
      RangeInt64(((Int64, RangeBoundKind)?, (Int64, RangeBoundKind)?))
      RangeDecimal(((String, RangeBoundKind)?, (String, RangeBoundKind)?))
      ArrayInt32(Array[Int?])
      ArrayInt64(Array[Int64?])
      ArrayDecimal(Array[String?])
      ArrayStr(Array[String?])
      Interval(Interval)
      DbNull
} derive()
///|
/// A set of database rows
pub(all) struct RowSet {
      columns : Array[Column]; rows : Array[Array[DbValue]]
} derive()

